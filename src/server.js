// 🔀 Proxy file for Railway compatibility
// Actual server code is in src/app/server.js
// This file exists to maintain backward compatibility with Railway's expectations

module.exports = require('./app/server');
